{
  "build": {
    "cmd": "pip install -r backend/requirements.txt"
  },
  "start": {
    "cmd": "python -m backend.db.migrations.run_migrations 2>&1 || echo 'Migration skipped'; exec uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8080}"
  }
}
