{
  "build": {
    "cmd": "cd frontend && npm ci && npm run build"
  },
  "aptPackages": ["nginx", "gettext-base"],
  "start": {
    "cmd": "envsubst '${BACKEND_URL}' < frontend/nginx.conf.template > /etc/nginx/conf.d/default.conf && cp -r frontend/dist/. /usr/share/nginx/html/ && nginx -g 'daemon off;'"
  }
}
